# Server Configuration
PORT=3000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# CORS Configuration
# For development: Use '*' to allow all origins, or specify exact origin(s)
# Examples:
#   CORS_ORIGIN=*                           (allows all origins)
#   CORS_ORIGIN=http://localhost:5173      (specific Vite dev server)
#   CORS_ORIGIN=http://localhost:3001      (specific client port)
# Note: Wildcard patterns like 'http://localhost:*' are NOT valid CORS syntax
CORS_ORIGIN=*
